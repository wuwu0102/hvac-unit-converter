name: Android APK Build

on:
  workflow_dispatch:
  push:
    branches:
      - main

jobs:
  build-android-apk:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 20

      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: 17

      - name: Install dependencies
        run: |
          if [ -f package-lock.json ]; then
            npm ci
          else
            npm install
          fi

      - name: Prepare Capacitor web assets
        run: npm run mobile:prepare

      - name: Ensure complete Android platform exists
        run: |
          if [ ! -f android/gradlew ] || [ ! -f android/app/build.gradle ] || [ ! -f android/settings.gradle ]; then
            echo "Android platform is missing or incomplete. Recreating android/ with Capacitor."
            rm -rf android
            npx cap add android
          else
            echo "Android platform exists."
          fi

      - name: Sync Android
        run: npx cap sync android

      - name: Build debug APK
        run: |
          cd android
          chmod +x ./gradlew
          ./gradlew assembleDebug

      - name: Upload debug APK
        uses: actions/upload-artifact@v4
        with:
          name: hvac-unit-converter-android-debug-apk
          path: android/app/build/outputs/apk/debug/app-debug.apk
